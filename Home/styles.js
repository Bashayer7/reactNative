import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  background: { flex: 1, width: "100%", height: "100%" },
  title: { color: "#000", fontSize: 25, textAlighn: "center" },
});

export default styles;
